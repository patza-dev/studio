export type Candidate = {
  name: string;
  bio: string;
  photoUrl: string;
};

export type TeamMember = {
  id: number;
  name: string;
  role: string;
  photoUrl: string;
};

export type Policy = {
  title: string;
  icon: string;
  description: string;
};
